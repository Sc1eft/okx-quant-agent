# OKX 量化交易系统 - 依赖
# 安装: pip install -r requirements.txt

# --- 核心 ---
python>=3.10
pandas>=2.0.0
numpy>=1.24.0
httpx>=0.25.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- 回测与计算 ---
scipy>=1.10.0       # 统计计算、优化

# --- 可视化 ---
matplotlib>=3.7.0
plotly>=5.15.0

# --- Agent ---
openai>=1.0.0       # DeepSeek 兼容接口

# --- 通知 ---
smtplib             # 标准库，邮件通知
requests>=2.28.0    # Webhook 通知

# --- 测试 ---
pytest>=7.4.0
pytest-cov>=4.1.0

# --- 可选：后续升级 ---
# polars>=0.18.0    # 替换 pandas 提速
# psycopg2-binary   # PostgreSQL
# websockets        # WebSocket 实时行情
